Execute Vertical Slice Architecture refactor on BookBud project. STRICT PROTOCOL: Work only on refactor/vertical-slice-architecture branch. Execute 5 phases sequentially. DO NOT proceed to Phase N+1 until Phase N is fully complete with zero build errors.

PHASE 1: PRODUCTS (BOOKS)
- Move BookController, BookService, BookRepository, Book.java, all Book DTOs to features/books/
- Update package declarations to edu.cit.colo.bookbud.features.books.*
- Update all import statements across codebase
- Move frontend: BookBrowse, BookDetails, BookManagement, bookService.js, book components to features/books/
- Update React import paths
- Run mvn clean compile && npm run build. Verify zero errors before proceeding.

PHASE 2: USERS  
- Move UserController, AuthController, UserService, AuthService, UserRepository, User.java, PasswordResetToken.java, RefreshToken.java, all User DTOs, security classes to features/users/
- Update package declarations and imports
- Move frontend: auth pages, Profile, Settings, authService.js, userService.js, AuthCard, GoogleAuthButton, ProtectedRoute, user context to features/users/
- Update React import paths
- Run build verification. Ensure zero errors before Phase 3.

PHASE 3: CATEGORIES
- Move WishlistController, WishlistService, WishlistRepository, Wishlist.java, all Wishlist DTOs to features/categories/
- Update package declarations and imports
- Move frontend: Wishlist, Categories pages, wishlistService.js, category components to features/categories/
- Update React import paths
- Run build verification. Ensure zero errors before Phase 4.

PHASE 4: STOCK
- Move TransactionController, PaymentController, TransactionService, PaymentService, TransactionRepository, PaymentRepository, Transaction.java, Payment.java, all Transaction/Payment DTOs to features/stock/
- Update package declarations and imports
- Move frontend: Checkout, TransactionHistory, PaymentMethods pages, transactionService.js, paymentService.js, Payment components to features/stock/
- Update React import paths
- Run build verification. Ensure zero errors before Phase 5.

PHASE 5: DASHBOARD
- Move AdminController, EarningsController, NotificationController, AdminService, NotificationService, NotificationRepository, Notification.java, all Admin/Notification DTOs, exception classes to features/dashboard/
- Update package declarations and imports
- Move frontend: Dashboard pages, admin pages (except BookManagement), adminService.js, notificationService.js, AlertModal, ConfirmModal, Footer, Navbar to features/dashboard/
- Update React import paths
- Run final build verification.

CRITICAL: No logic changes. Only file movement and import updates. After each phase, verify compilation succeeds and all tests pass. Complete refactor summary template in VERTICAL_SLICE_REFACTOR_INSTRUCTIONS.md when finished.
