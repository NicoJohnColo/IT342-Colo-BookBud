import { RouteObject } from 'react-router-dom';
import { ROUTES } from './constants';

export const routes: RouteObject[] = [
  {
    path: ROUTES.HOME,
    // element: <LandingPage />,
  },
  // Routes to be populated with actual components later
];
